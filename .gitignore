# OS / editor
.DS_Store
*.swp
*~

# Folio working state (drafts, snapshots)
.folio/
.tmp/

# folio inventory CLI の生成物 (REQ-VER-010、 generatedAt を含む作業用 output)。
# 検証の golden は verification/baselines/reference/ に track する。
inventory.json

# Claude Code session externalization (cc-session ready-compaction = .claude-session/)
.claude-session/
.mailbox/

# spawn 先 git worktree (session:spawn cld-spawn = worktrees/、 native claude --worktree = .claude/worktrees/)
worktrees/
.claude/worktrees/
