# folio plugin — agents/

Phase X1 placeholder. 8 specialist agents will be implemented in Phase X2.

## Planned specialist agents

| agent | phase | focus |
|---|---|---|
| `spec-explorer` | B (2-3 parallel) | cross-ref, ADR, EXP listing, related spec discovery |
| `spec-architect` | D (2-3 parallel, optional) | minimal / clean / pragmatic 3 design options |
| `spec-review-vocabulary` | F (parallel, axis 1) | canonical name violations, forbidden synonym detection |
| `spec-review-structure` | F (parallel, axis 2) | directory layout, cross-ref integrity, heading hierarchy |
| `spec-review-ssot` | F (parallel, axis 3) | content domain exclusivity (spec/ADR/changes/research) |
| `spec-review-temporal` | F (parallel, axis 4) | declarative form, past/future narration detection |
| `spec-review-ears` | F (parallel, axis 5) | EARS pattern completeness, REQ-ID uniqueness |
| `spec-review-stakeholder` | F (parallel, axis 6) | ISO 42010 Stakeholder Perspectives coverage |

## Reference

Spec for these agents is defined in [`architecture-rules.html` §7.2 Specialist Agents](../../architecture-rules.html).
