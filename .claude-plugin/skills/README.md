# folio plugin — skills/ (placeholder)

**正規の skill location は plugin root 直下 `skills/<name>/SKILL.md`** (Claude Code 公式仕様。 hooks/ を Phase 2.5 で plugin root に移動したのと同じ論理)。 `.claude-plugin/` 配下の skills/ は Claude Code に discover されない。

folio-architect SKILL は [`../../skills/folio-architect/SKILL.md`](../../skills/folio-architect/SKILL.md) に実装済 (Stage 1、 hybrid marker の正規 author entry point)。

本 dir は試作初期の placeholder。 P-11 部分隔離は scripts/ 等の HOW にのみ適用 (CLAUDE.md §6)。
