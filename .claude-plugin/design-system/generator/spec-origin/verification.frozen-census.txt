# folio-verification frozen census — policy A (folio-7n17 / ADR-0053)
# ============================================================================
# 政策 A (user 裁定 2026-07-18 = bootstrap 退役): snapshot oracle は「flip 時 1 回の
# bootstrap 忠実性証明」に縮退し、恒久防御は (a) literal census (本 file) + (b) contract
# 対 machine-block round-trip + (c) extractor-collapse 敵対 test が代替する。
#
# ★本 file の値は canonical / artifact / 生成物から【導出しない】凍結 literal (frozen literal)。
#   measured 側 (生成物 HTML = verify の subject) とのみ突合する。両側 contract 由来の相対 parity
#   (flip 後 ORIG==生成物 で自己比較恒真化) を撤去した後の唯一の独立 anchor。
#
# provenance:
#   canonical = design-intent/spec/verification.html @ base 2f53388b (srpz land 済)
#   snapshot(bootstrap) = spec-origin/verification.origin.html @ 83e07b6 (lwhz land)
#   ★ADR-0054 land (folio-9m1h / Cell V) で contract が成長したため provenance sha を現 contract へ re-freeze した
#     (census-guard (1) の要求)。 ★scalar 値と ID_SET は ★全て不変 — 提示層のみを動かす cell ゆえ census を 1 つも
#     bump しない (実測で確認: id 57 / a.xref 31 / span.term 27 / delta 5 / esc-xref 3 / code 43/2/79/124 /
#     span 60/1 / esc-code 0 / esc-span 2 / jq-S 2/2 / stakeholders LD 不変)。 contract の成長は ★表層 data field
#     2 種のみ (references[].title 30 / requirements[].priority 31) で、 machine_preamble / machine_blocks /
#     statement / essence の ★逐語は 1 byte も触れていない (Leg4 なし)。
#     ★提示層 wrapper の新 id 2 本 (forward-refs / glossary-terms) は verify-verification.sh の ★肯定形
#     allowlist 除外で FZ_ID_COUNT=57 / ID_SET を不変に保つ (admin 裁定 C2・default-block 維持)。
#     ★平易行 (rq-plain) / 優先度バッジ (rq-prio) / chip 一行タイトル (rf-gloss) の新設自由文は ★plain text のみで
#     著述し span.term / 人間層 <code> / a.xref を新たに生やさないため rich census も不変 (V確定3)。
#     ★spec-origin/verification.origin.html (pre-flip snapshot) は ★不改変 — 政策 A で ORIG は census / round-trip を
#     【消費しない】(存在 pin のみ) 一方、 test-adversarial-verification.sh の COLLAPSE 群は pre-flip 再抽出元として
#     ★内容を消費する ため、 再 freeze すると前提が壊れ、 かつ本 header の provenance (canonical @2f53388b /
#     snapshot(bootstrap) @83e07b6) が虚偽化する (V確定1b / wave 恒常 4 = 意図的に実施しない旨をここに開示する)。
#   ★ADR-0055 land (folio-h4nm) で REQ-VER-012 statement を改訂 (--boot = Tier 0 pointer digest の SHALL 条項追加 =
#     機械層逐語の正当変更・Leg4 型) したため provenance sha を現 contract へ re-freeze し、scalar 3 値を機械再導出で bump した
#     (2026-08-03 verify-verification.sh census arm 実測): FZ_XREF 31→32 (statement 内 ADR-0055 明示 a.xref +1) /
#     FZ_CODE_TABLE_CELL 43→44 (§3.7 RTM scenario cell の <code>prime-boot.yaml</code> +1) / FZ_CODE_REST 79→80・FZ_CODE_TOTAL 124→126 (RTM cell +1 と、apply-verifier BG-1 是正 = essence へ <code>--boot</code> 条項追加の +1)。
#     statement 内の新規 <code> (--boot ×3 等) は機械層ゆえ人間層 census の対象外 (counts 不変を実測で確認)。他 scalar / ID_SET 不変。
#   ★REQ-NAV-008 新設 (chrome-scope arm の REQ 正式化・ADR-0054 §2.1 由来・folio-4vfb) で contract requirements が 31→32 件へ
#     成長したため provenance sha を現 contract へ re-freeze し、scalar 5 値 + ID_SET を機械再導出で bump した
#     (2026-08-03 verify-verification.sh census arm 実測): FZ_ID_COUNT 57→58 (+req-nav-008) / FZ_CODE_TABLE_CELL 44→46
#     (§3.7 RTM 新行 scenario cell の <code>chrome-scope-clean</code> + <code>-violation.yaml</code> の +2) / FZ_CODE_REST 80→81
#     (essence の <code>[chrome-scope]</code> +1)・FZ_CODE_TOTAL 126→129 / FZ_SPAN_TABLE_CELL 60→62 (RTM 新行の badge span +
#     ears span の +2)。★FZ_XREF は 32 不変 — statement / essence の ADR-0054 言及は全て plain <a href> (class 無し) で書き、
#     essence 側の裸 token は fix Pass3 bare-token auto-wrap が xref 昇格させるため freeze 前に anchor 内へ統一した
#     (anchor 内は wrap mask 対象 = 不動点)。FZ_TERM / DELTA / ESC / JQS / stakeholders は不変。
#     ★同 land 内の Phase F ceiling errata (5-lens findings 統合) で REQ-NAV-008 essence/statement・REQ-VER-002 statement・
#     §3.8 導入 prose・§3.9 subhead・RTM derives-from・REQ-NAV-007 逆参照を改訂 (機械層逐語 + 人間層 essence の同時改訂)。
#     scalar は全て不変 (essence の <code> 数 1 個のまま・新規リンクは全て plain <a href>)・provenance sha のみ再 freeze。
#   provenance_contract_sha256 = fcb649c040407d3ad393d87262b1cd98ad0f7ca8d0b722bc30e21d7f6d11c995
#     (folio-verification.spec.yaml の freeze 時 sha256。数字上書き防壁 = census-guard.sh が
#      base..HEAD で「census 値変更 ⟺ contract 変更」の連動 + 本 sha 整合を検査する。deliverable 7)
# ============================================================================

# ---- scalars (frozen literal・subject = 生成物 HTML) ----
FZ_ID_COUNT=58
FZ_XREF=32
FZ_TERM=27
FZ_DELTA=5
FZ_ESC_XREF=3
FZ_CODE_TABLE_CELL=46
FZ_CODE_CAPTION=2
FZ_CODE_REST=81
FZ_CODE_TOTAL=129
FZ_SPAN_TABLE_CELL=62
FZ_SPAN_CAPTION=1
FZ_ESC_CODE=0
FZ_ESC_SPAN=2
FZ_JQS_TOTAL=2
FZ_JQS_MASKED=2
# JSON-LD folio:stakeholders の型+値 (ld_stake 出力形式: 型\t値・tab 区切り)。source せず fz_ld() で読む。
FZ_STAKEHOLDERS_LD=list	["Developer", "AI Agent", "External Reviewer"]

#BEGIN_ID_SET
req-nav-001
req-nav-002
req-nav-003
req-nav-004
req-nav-005
req-nav-006
req-nav-007
req-nav-008
req-ver-001
req-ver-002
req-ver-003
req-ver-004
req-ver-005
req-ver-006
req-ver-007
req-ver-008
req-ver-009
req-ver-010
req-ver-011
req-ver-012
req-ver-013
req-ver-014
req-ver-015
req-ver-016
req-ver-017
req-ver-018
req-ver-019
req-ver-020
req-ver-021
req-ver-022
req-ver-023
req-ver-031
s0-reader-guide
s1-1-concepts
s1-2-scope-boundary
s1-contract
s2-1-targets
s2-2-out-of-scope
s2-scope
s3-1-scenario-format
s3-10-flip-fidelity
s3-2-yaml-schema
s3-3-assertion
s3-4-baseline
s3-5-ears-mapping
s3-6-verification-specific
s3-7-coverage-rtm
s3-8-nav-req
s3-9-srs-done-condition
s3-ears
s4-1-3step
s4-2-completion-vs-prototype
s4-implementation
s5-1-gaps
s5-2-pending-adrs
s5-3-related-adrs
s5-open-questions
s6-references
#END_ID_SET

#BEGIN_DELTA_SET
D-2026-05-27-001
D-2026-05-27-002
#END_DELTA_SET
